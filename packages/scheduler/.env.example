# Service Configuration
NODE_ENV=development
PORT=9266
LOG_LEVEL=info

# Redis Configuration (required)
REDIS_URL=redis://localhost:6379

# API Configuration (required)
# Note: Use localhost:9264 for local development, api:3000 in Docker
API_URL=http://localhost:9264/graphql

# API Key for authenticating with the API service
# Format: camp_sk_<32_base64url_characters>
# This API key must be created via the API's /auth/api-keys endpoint or seed script
# Once generated, the key is shown only once - save it securely
# SECURITY WARNING: This default value is for development only and MUST be changed in production!
# To generate a key:
#   1. Start the API service
#   2. Use the seed script: pnpm --filter @campaign/api prisma:seed
#   3. Or create via API: POST /auth/api-keys with valid JWT token
# Example format: camp_sk_abc123xyz789def456ghi012jkl
API_SERVICE_ACCOUNT_TOKEN=camp_sk_example1234567890abcdefghijk

# Cron Schedules (optional, defaults shown)
CRON_EVENT_EXPIRATION=*/5 * * * *  # Every 5 minutes
CRON_SETTLEMENT_GROWTH=0 * * * *   # Every hour
CRON_STRUCTURE_MAINTENANCE=0 * * * * # Every hour

# Job Queue Configuration (optional, defaults shown)
QUEUE_MAX_RETRIES=3
QUEUE_RETRY_BACKOFF_MS=5000
QUEUE_CONCURRENCY=5

# API Client Configuration (optional, defaults shown)
API_REQUEST_TIMEOUT_MS=10000
API_CIRCUIT_BREAKER_THRESHOLD=5
API_CIRCUIT_BREAKER_DURATION_MS=30000
